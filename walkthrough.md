# Walkthrough

## 2026-03-11
- Initialized project tracking for the Vervet Network backend.
- Defined the first implementation plan around NestJS, PostgreSQL, Prisma, and recipient-first wallet resolution.
- Scaffolded a strict NestJS backend in `backend/` to keep room for a future split frontend/backend product layout.
- Replaced the Nest starter app with production-oriented modules for partner registration, signing key registration, signed attestation ingestion, destination resolution, and pasted-address verification.
- Added a PostgreSQL Prisma schema for partners, recipients, identifiers, asset networks, destinations, attestations, lookup logs, risk signals, webhooks, and audit entities.
- Generated the initial SQL migration artifact and added a Prisma seed script for the first chain/asset corridors.
- Added a Docker Compose-backed local PostgreSQL workflow plus backend docs and scripts for `migrate deploy` and seeding.
- Added partner API credential issuance, secret hashing/verification, scope-based bearer auth, and protection for the attestation write path.
- Added admin-token protection for partner bootstrap endpoints.
- Added partner-scoped webhook endpoint registration/listing plus signed webhook delivery for destination updates and revocations.
- Added webhook retry scheduling, exponential backoff, and an admin-triggered processor for pending deliveries.
- Added webhook endpoint lifecycle controls for update, soft delete/disable, and signing-secret rotation.
- Added structured audit-log emission for partner bootstrap actions, attestation ingestion, webhook endpoint mutations, and abandoned deliveries.
- Added an automated in-process webhook delivery worker with configurable interval and batch size.
- Added partner-scoped webhook delivery read APIs and partner-scoped audit-log read APIs for future dashboard consumption.
- Extended the audit model with explicit partner scoping so read APIs can query operational history reliably.
- Verified the local database workflow against a live PostgreSQL container by applying the initial migration and seeding core chain/asset data.
- Verified the authenticated end-to-end flow by creating a partner, registering a signing key, issuing an API credential, ingesting a signed attestation, and resolving/verifying the destination through the API.
- Verified the webhook flow end-to-end by registering a webhook endpoint, ingesting an attestation, observing a `SUCCEEDED` delivery in PostgreSQL, and recomputing the HMAC signature successfully against the captured webhook payload.
- Verified the Phase 7 operational flow end-to-end by forcing a failed webhook delivery to reschedule, rotating the endpoint signing secret, retrying the pending delivery to `SUCCEEDED`, then disabling the endpoint and confirming a later pending delivery was marked `ABANDONED` with matching audit records.
- Verified the Phase 8 operational flow end-to-end by forcing a failed delivery into a due state, letting the automated worker retry it to `SUCCEEDED`, then reading the result back through `GET /v1/webhooks/deliveries` and partner-scoped audit records through `GET /v1/audit-logs`.
- Hardened the resolution and attestation request paths with partner-scoped lookup idempotency, replay-protection nonces, freshness checks, rate limiting, and anti-enumeration controls.
- Added a Next.js dashboard foundation in `dashboard/` with encrypted session cookies, partner credential sign-in, and the first webhook-delivery and audit-log views against the live backend APIs.
- Verified the initial backend slice with Prisma validation/generation, `npm run build`, `npm test -- --runInBand`, `npm run test:e2e -- --runInBand`, and `npm run lint`.
- Verified the Phase 9 backend hardening end-to-end by applying the Prisma migration, rejecting nonce replays and stale attestation timestamps, enforcing resolution scopes, returning stored idempotent lookup responses, and blocking rate-limit and anti-enumeration scenarios in e2e coverage.
- Verified the dashboard foundation with `npm run lint`, `npm run build`, unauthenticated `/deliveries` redirect behavior, and authenticated page rendering against a live read-scoped partner credential.
- Extended the dashboard with partner-managed webhook endpoint operations, including endpoint creation, status changes, signing-secret rotation, one-time flash messaging, and a dedicated `/webhooks` operations screen.
- Verified the webhook dashboard slice with `npm run lint`, `npm run build`, authenticated `/webhooks` rendering, dashboard-driven endpoint creation, backend/API confirmation of the new endpoint, and dashboard-driven pause, resume, rotate-secret, and disable actions against the live backend.
- Removed the accidental nested `dashboard/.git` repository created by the Next.js scaffold so the dashboard now behaves as part of the main workspace.
- Added partner-scoped recipient registry APIs, recipient detail APIs, and attestation activity APIs on top of the materialized recipient/destination model created by attestation ingestion.
- Extended the dashboard with a recipient-first operations surface, including `/recipients` and `/recipients/[recipientId]` views for current destinations and recent attestation history.
- Verified the recipient operations slice with backend unit/e2e checks, dashboard lint/build, a live partner bootstrap plus two signed attestations, dashboard redirect to `/recipients`, recipient registry rendering, and recipient detail rendering with the rotated destination and attestation history.
- Extended the dashboard with a sender-facing `/resolution` console for recipient-first destination resolution and pasted-address verification against the intended recipient.
- Verified the sender console with dashboard lint/build, authenticated redirect to `/resolution`, a live resolve flow rendering the verified destination and `Safe To Send`, and a live mismatch verify flow rendering `MISMATCH`, `Do Not Send`, and `Address Mismatch`.
- Extended the backend with partner-scoped access-management APIs for partner profile reads, API credential lifecycle, signing-key lifecycle, and admin setup-token verification.
- Completed the dashboard end to end with an admin `/setup` onboarding flow, partner-scoped `/access` credential and signing-key management, dual sealed sessions for partner and admin setup contexts, and updated landing/navigation copy.
- Verified the full dashboard lifecycle with backend build/lint/unit/e2e checks, dashboard lint/build, a live admin setup flow creating a partner plus first key plus first credential, and a live `/access` flow issuing and revoking a secondary credential plus registering and revoking a second signing key.
- Applied the verified audit fixes across backend and dashboard: restricted CORS to configured origins, added Swagger docs, made attestation fanout durable by enqueueing webhook deliveries inside the attestation transaction, recovered stale `PROCESSING` deliveries, added production webhook target validation, moved credential verification off the synchronous hot path, debounced `lastUsedAt` writes, added nonce and historical resolution-request cleanup to the worker, tightened attestation corridor validation to pre-registered assets/networks only, improved address lookalike detection, and corrected validation responses that were returning `409` instead of `400`.
- Added dashboard request timeouts plus root and ops `error.tsx` / `loading.tsx` boundaries, and reduced login-time backend probing to the partner profile plus scope checks.
- Added targeted backend coverage for private webhook target rejection, stale delivery recovery, improved lookalike detection, async API credential verification, and attestation rejection for unregistered asset-network corridors.
- Replaced the partner dashboard auth model so humans now sign in with partner user email/password accounts while API credentials remain integration-only credentials.
- Added partner-user and dashboard-session persistence in Prisma, admin bootstrap for the first owner user, password-based dashboard login/logout endpoints, and dashboard-session authentication alongside existing API credential auth for partner operations routes.
- Rewired the Next.js dashboard to use human sign-in, encrypted session cookies that store dashboard session tokens plus user metadata, owner-user onboarding at `/setup`, and partner-ops pages/actions that now authenticate with the dashboard session token.
- Verified Phase 16 with Prisma validation/generation, local migration deploy, backend build/lint/unit/e2e checks, dashboard lint/build, and a live runtime check confirming the running dashboard renders the new email/password login flow.
- Completed the PRD-aligned dashboard redesign with a grouped partner-console shell, legacy route redirects, overview, split resolution flows, destinations, attestations, webhook detail/delivery pages, access split into API keys/signing keys/team/security, audit exports, docs, and sandbox.
- Added the backend surfaces required by the redesign, including overview aggregation, resolution logs and batch verify, destination detail and lifecycle endpoints, attestation detail, webhook test/replay/detail reads, partner team and security settings APIs, and audit export jobs.
- Added Playwright as a dashboard dev dependency to support browser-level verification of the redesigned UI and future regression checks.
- Ran a live browser QA pass against the rebuilt dashboard on `http://localhost:3001`, including sign-in, legacy redirects, route-by-route rendering, resolution success/mismatch flows, batch verification interaction, and the mobile navigation drawer.
- Fixed a runtime dashboard defect discovered during live QA where `VERVET_API_BASE_URL` was double-prefixing API requests as `/v1/v1/...`; the client now normalizes either base URL form and the local/example env defaults now point at `http://localhost:3000`.
- Added the V2 resolution model end to end: reverse lookup (`By Address`) in the backend, dual-mode transfer verification, split overview metrics for by-recipient/by-address/verify-transfer usage, resolution logs with platform and disclosure metadata, and the renamed dashboard resolution surface (`By Recipient`, `By Address`, `Verify Transfer`, `Batch Verify`, `Logs`).
- Extended the dashboard and shared client types with disclosure mode, lookup direction, reverse-lookup confirmation cards, resolution availability cards, and the new docs/sandbox taxonomy so the partner console now reflects forward lookup, reverse lookup, and transfer verification explicitly.
- Verified Phase 19 with Prisma validation/generation, a live migration deploy, backend build/lint/unit/e2e checks, and dashboard lint/build. Local servers were not running at closeout, so this pass did not include an additional browser smoke test.
- Added a partner-facing supported-platforms API at `/v1/platforms` and used it to replace free-text platform entry with dropdown-backed selection in the `By Address` and address-context `Verify Transfer` flows.
- Added backend e2e coverage for supported-platform capability flags (`supportsByAddress`, `supportsByRecipient`) and verified that unsupported or disabled partners are excluded from the returned list.
- Verified Phase 20 with backend build/lint/e2e and dashboard lint/build. The existing backend Jest open-handle warning still prints after successful e2e completion.
- Curated the supported platform directory so dropdown-backed platform selection now only returns intentionally listed demo platforms instead of arbitrary partner/test tenants.
- Added realistic local seed data for Binance, Bybit, Coinbase, Trust Wallet, and Ivorypay, plus live recipients, destinations, attestations, webhook activity, audit activity, overview data, and human dashboard logins for Ivorypay.
- Updated the platform selector to show clean public-facing platform labels and replaced the remaining platform text filter in the dashboard logs with the shared dropdown-backed selector.
- Fixed the supported-platform e2e test cleanup so temporary `phase20-*` fixtures no longer leak into the shared local database after test runs, and hardened the seed script to reset non-curated partners out of the public directory on every reseed.
- Verified Phase 21 with backend build/lint, targeted platform e2e, reseeding, live login against `/v1/dashboard-auth/login`, and a live `/v1/platforms` response containing only the curated platform set.
- Started V3 of the external platform dashboard by adding organization-level capability flags, onboarding stage, and feed health state to the `Partner` model and exposing that context through the existing `GET /v1/partners/me` profile response.
- Added partner capability, onboarding, and readiness summaries to the external dashboard shell and Overview so the workspace now reflects whether the organization is an API consumer, a data partner, production-approved, or still progressing through setup.
- Added capability-aware sidebar gating and direct-route restricted-module banners for registry, webhook, signing-key, audit-export, and sandbox entry pages, so the external console now adapts to organization type instead of assuming all partners are the same.
- Updated the partner role scope bundles so analyst and read-only users also receive `partners:read`, allowing the shell to load shared organization context without breaking lower-privilege sessions.
- Verified the first V3 phase with Prisma validation/generation, a live migration deploy, backend build/lint/unit/e2e checks, reseeding, dashboard lint/build, and a live `GET /v1/partners/me` response showing the new capability/onboarding/readiness payload for the seeded Ivorypay demo account.

## 2026-03-13
- Completed the second V3 external-dashboard phase by turning `/setup` into a dual-purpose route: authenticated partner users now see the onboarding home and adaptive checklist, while the existing unauthenticated admin-token bootstrap workspace remains intact for platform setup.
- Added setup discoverability to the external shell and Overview so incomplete organizations now get a direct `Continue setup` path from the sidebar context rail, Overview actions, and empty-state guidance instead of being sent to generic quickstart docs.
- Repointed capability-gated module banners for recipients, destinations, attestations, webhooks, signing keys, audit exports, and sandbox into `/setup`, so restricted modules now send partners into guided onboarding rather than static documentation.
- Fixed the onboarding-home typing bug around `nextRecommendedAction` by adding a checklist-task type guard, then verified the updated dashboard with `npm run lint` and `npm run build`.
- Added a new backend `GET /v1/data-feed-health` aggregate plus a new external `/data-feed-health` page so data partners can monitor stale destinations, expiring attestations, failed deliveries, active signing keys, and feed-readiness recommendations from one place.
- Reused the same feed-health payload across the V3 dashboard by adding a first-class `Data Feed` nav item, a shared `FeedHealthCard`, and feed-health summaries on Overview, Recipients, Destinations, and Attestations.
- Added backend e2e coverage for the data-feed-health endpoint and verified the phase with backend build/lint/unit/e2e, dashboard lint/build, and live route/API smokes against `http://localhost:3001/data-feed-health` and `http://localhost:3000/v1/data-feed-health`.
- Tailored the external docs, quickstart, API reference, webhook guide, and sandbox pages by partner journey so API consumers, data partners, and combined partners now see different recommended steps, relevant workflows, and upgrade guidance.
- Added a shared partner-guidance layer plus a reusable production-upgrade card so docs and sandbox surfaces now connect onboarding state, capability profile, and production-readiness actions back to `/setup` and `/data-feed-health`.
- Verified the Phase 26 dashboard slice with `npm run lint` and `npm run build`.
- Added a full production approval workflow to the backend with partner-scoped request listing/creation/cancellation, admin-token review/approval, persisted approval history, and production state transitions that now feed the enriched partner profile payload.
- Added a new backend e2e suite covering owner approval requests, pending-request cancellation, admin review to production-approved state, and rejection of approval attempts from API credentials or lower-privilege dashboard roles.
- Extended the external setup and security surfaces with approval-status visibility, role-sensitive request and cancel controls, and production-readiness messaging that now reflects pending review, rejected review, or approved production state.
- Verified Phase 27 with Prisma validation/generation, local migration deploy, backend build/lint/unit/e2e checks, and dashboard lint/build.
- Added admin-token partner list and production-approval queue APIs plus a guarded admin-state update endpoint so the dashboard can now render an actual internal review console instead of a bootstrap-only setup page.
- Rebuilt the admin `/setup` workspace with a live pending-approval queue, approve/reject actions, a partner roster, and controlled capability/readiness controls for partner status, onboarding stage, feed health, and non-production capability flags.
- Added server action routes for admin approval reviews and partner-state updates, plus a new backend e2e suite covering partner listing, approval-queue reads, and admin-controlled partner state transitions.
- Verified Phase 28 with backend build/lint/unit/e2e checks, dashboard lint/build, and the existing full e2e run; the pre-existing Jest open-handle warning still appears after successful backend e2e completion.
- Added corridor-scoped production governance to the domain model with `PartnerProductionCorridor`, new admin endpoints for corridor listing and grant/revoke actions, partner-facing reads for granted production corridors, and seed support for production-ready demo partners.
- Extended the partner profile and admin partner workspace payloads with corridor-aware production access summaries so readiness now reflects both org-wide production approval and the number of granted production asset-network corridors.
- Rebuilt the admin `/setup` workspace to show granted corridors for the selected partner and to support corridor grant/revoke actions directly from the dashboard, including a curated available-corridor dropdown and flash-backed action routes.
- Extended the external onboarding home and readiness surfaces so partners can now see whether they are production approved but still blocked by missing corridor grants.
- Fixed a stray backend helper script that still referenced pre-refactor Prisma models and cleaned up an unrelated demo-page lint violation so the verification pass returned to a clean build/lint state.
- Verified Phase 29 with Prisma validation/generation, a real migration deploy, reseeding, backend build/lint/unit/e2e checks, and dashboard lint/build. The existing backend Jest open-handle warning still appears after successful full e2e completion.
- Added corridor-aware production approval scope to the data model with `PartnerProductionApprovalRequestCorridor`, so approval requests can now persist the exact asset-network corridors a partner wants to launch.
- Extended partner-facing setup so owners/admins can select requested production corridors during the approval request flow, and updated recent approval history to show the requested corridor scope.
- Extended the admin review queue and selected-partner view in `/setup` so reviewers can see the requested corridor scope before approving or rejecting a request.
- Updated the backend approval workflow so approving a production request now automatically grants the requested corridors via `PartnerProductionCorridor` records instead of leaving corridor grants disconnected from the review decision.
- Added backend e2e coverage for requesting approval with corridor scope and verified that approving the request updates readiness to production-approved with the expected granted corridor count.
- Verified Phase 30 with Prisma validation/generation, a real migration deploy, reseeding, backend build/lint/unit/e2e checks, and dashboard lint/build. The existing backend Jest open-handle warning still appears after successful full e2e completion.
- Added approval-time corridor scope overrides with a new `PartnerProductionApprovalApprovedCorridor` persistence layer, so admin reviewers can now approve a narrowed or altered corridor set without mutating the original requested scope.
- Extended the admin `/setup` review workspace to show requested corridor scope, allow checkbox-based approved-scope overrides during approval, and show the latest approved scope on the selected-partner detail surface.
- Extended partner onboarding history and audit event detail views so corridor-native production review history now shows requested-versus-approved scope instead of collapsing everything into a single granted-corridor summary.
- Added a partner-facing available-production-corridors read endpoint to support the corridor-aware approval flow without leaking admin-only partner state.
- Added the Phase 31 migration artifact, cleared an unrelated `/demo` page lint blocker, and verified the phase with Prisma validation/generation, a real migration deploy, reseeding, backend build/lint/unit/e2e checks, and dashboard lint/build. The existing backend Jest open-handle warning still appears after successful full e2e completion.
- Added a metadata-driven partner access policy layer to the existing auth guard so partner-facing backend routes can now enforce capability flags, minimum onboarding stage, actor type, and allowed partner-user roles without duplicating logic across services.
- Applied the new backend enforcement layer across V3-sensitive routes including resolution, platforms, recipients, destinations, attestations, data-feed health, webhooks, audit exports, and sensitive `/v1/partners/me` self-service endpoints.
- Refined dashboard access behavior for API Keys and Signing Keys so those surfaces now align with the intended V3 role model: owners, admins, and developers can manage them, while lower-privilege users no longer see the nav items or fall through into backend 403s.
- Updated the developer dashboard scope bundle so developers now receive `partners:write`, matching the intended ability to manage API keys and signing keys while still being blocked from owner/admin-only production and security actions by the new backend role policy.
- Added focused backend e2e coverage for capability denial, batch-capability denial, onboarding-stage denial, analyst denial for API key management, and owner-only security-policy writes, then updated older attestation/signing-key fixtures to satisfy the new policy requirements.
- Verified Phase 32 with backend build/lint/unit/e2e checks plus dashboard lint/build. The existing backend Jest open-handle warning still appears after successful full e2e completion.
- Expanded `GET /v1/data-feed-health` into a real operational feed-health aggregate that now includes corridor-level freshness, recent attestation-ingestion activity, recent ingestion failures, webhook test failure history, and richer attention metrics alongside the existing stale-destination and stale-attestation drilldowns.
- Added failed attestation-ingestion audit logging on rejected attestation writes so feed-health can now surface real partner-scoped ingestion failure history going forward instead of only accepted trust events.
- Rebuilt the dashboard `/data-feed-health` route around the richer payload with corridor health, ingestion history, combined failure history, and updated summary cards, then surfaced the new degraded-corridor and ingestion-failure signals in Overview and the main registry pages through the shared feed-health card and summary surfaces.
- Added backend e2e coverage proving that failed attestation-ingestion attempts appear in feed-health history, and verified Phase 33 with backend build/lint/unit/e2e checks plus dashboard lint/build. The existing backend Jest open-handle warning still appears after successful full e2e completion.
- Tightened the remaining V3 state-sensitive enforcement gaps so partners without sandbox or production access can no longer execute live resolution flows or list supported platforms, even if they still have the underlying resolution capability enabled.
- Brought full-attestation-only partners into parity with other data-contributing organizations across resolution testing, partner self-service API credential management, onboarding guidance, and registry access semantics.
- Updated dashboard availability handling for API Keys and Signing Keys so those pages now render explicit state-aware guidance instead of relying on narrow legacy capability checks.
- Added automated backend state-matrix coverage for restricted-environment denial and full-attestation-only partner behavior, then verified Phase 34 with backend build/lint/unit/e2e checks plus dashboard lint/build. The existing backend Jest open-handle warning still appears after successful full e2e completion.
- Added hidden QA demo organizations and stable partner-user credentials for consumer-only, data-only, degraded, and restricted dashboard states without polluting the public supported-platform directory.
- Added a Playwright-based browser matrix for the external dashboard and verified the seeded org-state experience across consumer-only, data-only, combined production, degraded, and restricted partners.
- Verified Phase 35 with reseeding, backend build, dashboard lint/build, and `playwright test` against the live dashboard stack. The matrix passed cleanly after tightening the test assertions around duplicated shell text.
- Added a stable developer demo user for the combined Ivorypay partner and seeded a deterministic pending production-approval request for the consumer demo partner so the admin `/setup` review queue is always populated during browser QA.
- Added a second Playwright browser matrix for role-sensitive and admin-review flows, covering owner, analyst, developer, and admin `/setup` experiences against the live dashboard.
- Verified Phase 36 with reseeding, dashboard lint/build, and `playwright test` against the new role/admin matrix. The suite passed after tightening the admin setup assertions to target the correct review card, selected-partner card, and state-update button instead of colliding on repeated text labels.
- Fixed the lingering backend Jest e2e open-handle warning by forcing the e2e runner into `NODE_ENV=test`, disabling the webhook worker explicitly in the lightweight app e2e suite, and allowing Prisma’s test-time Postgres pool to exit on idle.
- Verified Phase 37 with reseeding, `npm run test:e2e -- --runInBand`, backend build, and backend lint. The plain e2e command now exits cleanly without the previous Jest shutdown warning.
- Traced the lingering backend `pg` deprecation warning to the production-approval workflow and removed it by making those Prisma transactions write-only, then hydrating approval-request responses after commit instead of reading relation-heavy payloads inside the transaction.
- Verified Phase 38 with traced single-suite execution for `production-approval.e2e-spec.ts`, then a full reseeded backend e2e run, build, and lint. The backend test path now runs without the previous `pg` deprecation warning.
- Added a mutation-focused Playwright suite for real dashboard write flows, covering owner-side API key issuance/revocation, team invite creation, webhook creation plus endpoint lifecycle actions, and admin `/setup` partner creation plus state/corridor updates.
- Fixed a browser-test infrastructure bug where Playwright used `127.0.0.1` while redirect-based dashboard actions resolved to `localhost`, which dropped the host-bound session cookie during form submissions; the Playwright config now uses `localhost` consistently.
- Tightened the new mutation suite selectors so dense admin and webhook screens are scoped to the correct forms and detail cards instead of colliding with repeated labels and controls.
- Verified Phase 39 with backend reseeding, dashboard lint/build, and `playwright test tests/e2e/partner-mutation-flows.spec.ts`. The suite now passes end to end against the live dashboard stack.
- Added a browser suite for trust-object lifecycle mutations, covering owner-side recipient creation and disable plus destination creation, replacement, and revocation through the live dashboard forms.
- Added a browser suite for corridor-scoped production governance, covering admin approval with corridor-scope override plus follow-up corridor revoke and re-grant flows inside `/setup`.
- Tightened the new browser suites to scope destination creation away from list filters and to assert admin governance state transitions without depending on label-format differences between approval checkboxes and granted-corridor display strings.
- Verified Phase 40 with reseeding, dashboard lint/build, and `playwright test` against both `partner-trust-object-mutations.spec.ts` and `admin-production-governance.spec.ts`. Both suites pass cleanly against the live dashboard stack.
- Added a browser suite for access and security mutations, covering developer-side signing-key registration/revocation and owner-side security-settings persistence through the live dashboard forms.
- Added a browser suite for partner onboarding approval flows, covering admin creation of a first owner account for a new partner, successful first-owner sign-in, satisfaction of API-consumer approval prerequisites, and partner-side production approval request/cancel from `/setup`.
- Tightened the new browser suites by generating a real ED25519 public key in the signing-key test and by moving the approval flow onto a freshly browser-created partner instead of relying on mutable demo-org review state.
- Verified Phase 41 with reseeding, dashboard lint/build, and `playwright test` against both `partner-access-security-mutations.spec.ts` and `partner-onboarding-approval.spec.ts`. Both suites pass cleanly against the live dashboard stack.
- Consolidated the growing external browser matrix into a deterministic ordered regression path by adding `dashboard/scripts/run-external-playwright.mjs`, which executes each Playwright suite in a fixed sequence and reuses the running local backend/dashboard stack instead of relying on Playwright’s internal file scheduling.
- Removed the remaining aggregate-run flake from the admin `/setup` path by introducing a sealed admin-session test helper for the suites that need the internal review workspace, so those browser tests now exercise the workspace itself without depending on the token-entry form as a prerequisite.
- Verified Phase 42 with reseeding, dashboard lint, the existing dashboard build, and `npm run test:playwright:external`. The full external dashboard browser matrix now passes cleanly end to end after a fresh seed.
- Added a dedicated Playwright smoke test for the `/setup` admin token entry flow, covering both successful workspace access and inline error handling for an invalid token, so the token-entry UX remains covered even though the main regression suites now use sealed admin sessions.
- Added a GitHub Actions workflow at `.github/workflows/external-dashboard-regression.yml` that provisions PostgreSQL, installs backend/dashboard dependencies, migrates and seeds the database, builds the dashboard, and runs both the new admin-setup-entry smoke and the full external browser regression path.
- Verified Phase 43 with backend reseeding, dashboard lint/build, `npm run test:playwright -- tests/e2e/admin-setup-entry.spec.ts`, and `npm run test:playwright:external`. Both the isolated smoke and the aggregate browser matrix pass locally against the running stack.
- Added a dedicated backend CI workflow at `.github/workflows/backend-quality.yml` that now covers Prisma validation/generation, backend build, backend lint, backend unit tests, and backend e2e tests against PostgreSQL in GitHub Actions.
- Split the external dashboard browser workflow into three separate jobs: the admin setup entry smoke, a state-oriented browser regression job, and a mutation-oriented browser regression job, so CI no longer forces the full Playwright surface through one monolithic runner.
- Extended `dashboard/scripts/run-external-playwright.mjs` to accept an explicit subset of the canonical suite list and added `test:playwright:external:state` plus `test:playwright:external:mutations` package scripts for the new split workflow jobs.
- Verified Phase 44 with Prisma validation/generation, backend reseeding, backend build/lint/unit/e2e, dashboard lint/build, `npm run test:playwright:admin-setup-entry`, `npm run test:playwright:external:state`, and `npm run test:playwright:external:mutations`.

## 2026-03-14
- Added backend-owned dashboard metadata under the partner module so the frontend can fetch corridors, option sets, onboarding task definitions, partner guidance, sandbox presets, and sample response data from the API instead of maintaining local copies.
- Added `GET /v1/partners/me/dashboard-metadata` for authenticated partner sessions and `GET /v1/partners/setup/metadata` for the admin `/setup` workspace, plus backend e2e coverage for both metadata routes.
- Enriched the partner profile payload with a backend-owned next recommended onboarding action label and production-approval blocked-reason description so setup and upgrade surfaces no longer depend on duplicated frontend copy maps.
- Replaced the remaining hardcoded dashboard option lists across resolution, recipients, destinations, attestations, webhooks, access, audit, and admin setup forms with metadata fetched through `dashboard/src/lib/vervet-api.ts`.
- Replaced hardcoded onboarding checklist definitions, upgrade guidance, and sandbox defaults with backend metadata in the setup, docs, and sandbox surfaces.
- Removed the obsolete frontend-only metadata sources `dashboard/src/lib/network-options.ts` and `dashboard/src/lib/partner-guidance.ts` once all consumers were migrated.
- Verified Phase 45 with backend build/lint/unit/e2e and dashboard lint/build.
- Added backend-owned data-partner ingestion guidance to dashboard metadata, including the signed attestation flow, operational notes, an example `/v1/attestations` payload, and a cURL request template.
- Added a reusable dashboard ingestion-guide panel and rendered it in `/setup`, `/docs/api`, and `/data-feed-health` when a partner is still waiting for the first signed trust update.
- Verified Phase 46 with backend build/lint plus targeted metadata e2e coverage, and dashboard lint/build.
- Hardened the GitHub Actions workflows by adding path-aware triggers and `concurrency` controls, splitting backend CI into a fast no-database lane plus a PostgreSQL-backed e2e lane, and refactoring the external dashboard browser workflow into a shared matrix job with Playwright browser caching.
- Added the first CD scaffold at `.github/workflows/staging-deploy.yml`, which builds backend and dashboard staging bundles, uploads release artifacts plus a manifest, optionally calls a staging deployment webhook, and can run a post-deploy health check when repo configuration is present.
- Verified Phase 47 by statically validating `.github/workflows/backend-quality.yml`, `.github/workflows/external-dashboard-regression.yml`, and `.github/workflows/staging-deploy.yml`, and by fixing the staging manifest to emit a real UTC timestamp instead of reusing the GitHub run ID.
